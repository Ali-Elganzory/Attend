import 'package:flutter/material.dart';

class StudentInfo extends StatelessWidget {
  @override
  final String name;
  final String id;
  final int number;
  StudentInfo({this.number,this.name,this.id});
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}