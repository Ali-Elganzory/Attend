import 'package:flutter/material.dart';

class NameList extends StatelessWidget {
  final String name;
  final String id;
  NameList({this.name,this.id});
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}